Business Logic internal function calls:


1.	// select the  current location
       Call to BL to get location GetUserLocation( )
{
//calculates the user location
GetUserLocation(string location, float latitude, float longitude)
}
Call to DAL GetUserLocation( )
{
//get the location from DB using DAL
//DAL return the location address, latitude and longitude

2.	//User provide destination
Call to BL from UL
FindLocationFRomDestination(StringName)
{
// call to DB with current location and string Name

GetInitialLocationandDestinationFromInput(String userinput, currentLocation)
DB return to BL
}


3.	// Select Building Type

Call to BL from UI
GetBuildingType( )
{
// get the building type from DB and respond to UI
// returns building model, height and width
}
